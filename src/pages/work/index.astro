---
import Layout from "../../layouts/Layout.astro";
import { getProjects } from "../../lib/cms";
import { absoluteUrl, pageSchema } from "../../lib/seo";

export const prerender = true;

const sortedProjects = await getProjects();
const workTitle = "Work | Phuc Loc Nguyen";
const workDescription = "Selected projects by Phuc Loc Nguyen: paid media scaling, TikTok Shop growth, SEO, lead generation, and community strategy.";
const workHeadlineHtml = "Selected work & <em>case studies</em>";
---

<Layout
  title={workTitle}
  description={workDescription}
  schema={[
    pageSchema({
      url: absoluteUrl("/work"),
      title: workTitle,
      description: workDescription,
      type: "CollectionPage",
    }),
    {
      "@type": "ItemList",
      "@id": `${absoluteUrl("/work")}#projects`,
      name: "Selected case studies",
      itemListElement: sortedProjects.map((project, index) => ({
        "@type": "ListItem",
        position: index + 1,
        url: absoluteUrl(`/work/${project.slug}`),
        name: project.title,
        description: project.summary,
      })),
    },
  ]}
>
  <main class="gear-page">
    <div class="gear-container">

      <!-- PAGE HERO -->
      <section class="gear-hero work-hero" aria-label="My work">
        <div class="page-kicker-wrapper">
          <span class="page-kicker-text">My work</span>
        </div>
        <h1 class="gear-hero-title" set:html={workHeadlineHtml} />
        <p class="gear-intro">
          Paid media scaling, TikTok Shop growth, technical SEO,
          lead generation, and community work across e-commerce,
          education, and entertainment.
        </p>
      </section>


      <!-- Project List -->
      <div class="project-list">
        {sortedProjects.map((project) => (
          <article class="project-row">
            <div class:list={["project-row-inner", !project.coverImage && "project-row-inner--text-only"]}>

              <!-- Left: text -->
              <div class="project-text">
                <span class="project-number">{project.number} / {project.client.toUpperCase()}</span>
                <a href={`/work/${project.slug}`} class="project-title-link">
                  <h2 class="project-name">{project.title}</h2>
                </a>
                <div class="project-summary">{project.summary}</div>

                <p class="project-tools">
                  {project.tags.map((tag) => (
                    <span class="tag">{tag}</span>
                  ))}
                </p>

              </div>

              {project.coverImage && (
                <!-- Right: visual card -->
                <div class="project-visual">
                  <a href={`/work/${project.slug}`} class="project-card-link" aria-label={`View ${project.title}`}>
                    <div class="project-card-img">
                      <div class="card-logo-wrapper">
                        <img
                          src={project.coverImage}
                          alt={project.client}
                          class="card-logo"
                          loading="lazy"
                          decoding="async"
                        />
                      </div>
                      <div class="card-overlay">
                        <span class="card-cta">View project</span>
                      </div>
                    </div>
                  </a>
                </div>
              )}

            </div>
            <div class="project-divider"></div>
          </article>
        ))}
      </div>

    </div>
  </main>
</Layout>

<style>
  .projects-page {
    padding: calc(var(--hdr-h) + 80px) 0 120px;
  }

  /* ------ Page header ------ */
  .page-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: end;
    margin-bottom: 64px;
  }

  .page-title {
    font-family: var(--serif);
    font-size: var(--type-display-xl);
    font-weight: 400;
    line-height: 1.0;
    letter-spacing: -0.03em;
    color: var(--text);
    margin-bottom: 8px;
  }

  .page-subtitle {
    font-size: var(--type-ui);
    color: var(--muted);
    font-family: var(--mono);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .page-desc {
    font-size: var(--type-body);
    line-height: 1.72;
    color: var(--muted);
    max-width: 400px;
    align-self: end;
  }


  /* ------ Project list ------ */
  .work-hero {
    margin-bottom: clamp(48px, 7vw, 76px);
    padding-bottom: clamp(36px, 5vw, 56px);
    border-bottom: 1px solid var(--divider);
  }

  .work-hero .gear-intro {
    max-width: 560px;
    margin-top: 18px;
  }

  .work-hero .gear-hero-title {
    white-space: pre-line;
  }

  .project-list {
    display: flex;
    flex-direction: column;
    border-bottom: 1px solid var(--divider);
  }

  .project-row {
    content-visibility: visible;
    outline: none;
    transition: opacity 0.4s ease, filter 0.4s ease, transform 1s cubic-bezier(0.4, 0, 0.2, 1);
    will-change: opacity, filter, transform;
    cursor: pointer;
  }

  /* Hover effect: blur other rows when hovering one */
  @media (hover: hover) and (pointer: fine) {
    .project-list:hover .project-row:not(:hover) {
      opacity: 0.35 !important;
      filter: blur(4px) !important;
    }
  }

  .project-row-inner {
    display: grid;
    grid-template-columns: minmax(0, 0.88fr) minmax(320px, 0.82fr);
    gap: clamp(44px, 6vw, 84px);
    align-items: center;
    padding: clamp(44px, 6vw, 72px) 0;
  }

  .project-row-inner--text-only {
    grid-template-columns: minmax(0, 860px);
  }

  /* ------ Text side ------ */
  .project-number {
    display: block;
    font-family: var(--mono);
    font-size: var(--type-caption);
    color: color-mix(in srgb, var(--text) 68%, var(--muted));
    letter-spacing: 0.04em;
    text-transform: uppercase;
    margin-bottom: 16px;
  }

  .project-title-link {
    text-decoration: none;
    display: inline-block;
  }

  .project-title-link:hover .project-name {
    color: var(--accent);
  }

  .project-name {
    font-family: var(--serif);
    font-size: var(--type-card-title);
    font-weight: 400;
    line-height: 1.2;
    letter-spacing: -0.015em;
    color: var(--text);
    margin-bottom: 14px;
    transition: color 0.25s ease;
  }

  .project-summary {
    font-family: var(--sans);
    font-size: 0.875rem;
    font-weight: 400;
    line-height: 1.72;
    color: #6e6e6e;
    margin-bottom: 20px;
    max-width: 620px;
    text-wrap: pretty;
  }

  .project-tools {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 26px;
  }

  .tag {
    font-family: var(--mono);
    font-size: var(--type-micro) !important;
    letter-spacing: 0.02em;
    color: color-mix(in srgb, var(--text) 72%, var(--muted));
    border: 1px solid var(--divider);
    border-radius: 3px;
    padding: 4px 8px;
    text-transform: uppercase;
    transition: color 0.2s ease, border-color 0.2s ease;
  }

  .project-row:hover .tag {
    color: var(--text);
    border-color: #bbb;
  }

  .project-links {
    display: flex;
    align-items: center;
    gap: 24px;
  }

  .link-primary {
    font-size: 12px;
    font-weight: 500;
    color: var(--text);
    text-decoration: underline;
    text-underline-offset: 4px;
    text-decoration-thickness: 1px;
    text-decoration-color: var(--divider);
    transition: text-decoration-color 0.25s ease, color 0.25s ease;
  }

  .link-primary:hover {
    color: var(--accent);
    text-decoration-color: var(--accent);
  }

  .link-secondary {
    font-size: 12px;
    font-family: var(--mono);
    color: color-mix(in srgb, var(--text) 72%, var(--muted));
    transition: color 0.25s ease;
  }

  .link-secondary:hover {
    color: var(--accent);
  }

  /* ------ Visual card ------ */
  .project-card-link {
    display: block;
    text-decoration: none;
    border-radius: var(--rounded-md);
  }

  .project-card-img {
    --thumb-radius: 12px;
    --thumb-border-width: 0px;
    --thumb-inner-radius: var(--thumb-radius);

    position: relative;
    aspect-ratio: 2 / 1;
    background: rgba(255, 255, 255, 0.72);
    border-radius: var(--thumb-radius);
    overflow: hidden;
    border: 0;
    box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.52), 0 12px 32px rgba(28, 28, 28, 0.055);
    transition: border-color 180ms var(--ease-out), background 180ms var(--ease-out), box-shadow 180ms var(--ease-out);
  }

  .project-card-link:focus-visible .project-card-img {
    border-color: var(--accent-border);
    background: var(--accent-soft);
  }

  .card-logo-wrapper {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    border-radius: var(--thumb-inner-radius);
  }

  @media (max-width: 600px) {
    .card-logo-wrapper {
      padding: 0;
    }
  }

  .card-logo {
    width: 100%;
    height: 100%;
    object-fit: contain;
    border-radius: var(--thumb-inner-radius);
  }

  .card-overlay {
    position: absolute;
    inset: 0;
    border-radius: var(--thumb-inner-radius);
    background: rgba(28, 28, 28, 0);
    display: flex;
    align-items: flex-end;
    padding: 24px;
    transition: background 0.4s ease;
  }

  .card-cta {
    font-size: var(--type-ui);
    font-weight: 500;
    color: #fff;
    opacity: 0;
    pointer-events: none;
  }

  .project-card-link:focus-visible .card-overlay {
    background: rgba(28, 28, 28, 0.18);
  }

  /* ------ Divider ------ */
  .project-divider {
    height: 1px;
    background: var(--divider);
  }

  .project-row:last-child .project-divider {
    display: none;
  }

  /* ------ Responsive ------ */
  @media (max-width: 900px) {
    .page-header {
      grid-template-columns: 1fr;
      gap: 20px;
      margin-bottom: 48px;
    }

    .page-desc {
      max-width: 100%;
    }

    .project-row-inner {
      grid-template-columns: 1fr;
      gap: 32px;
      padding: 48px 0;
    }

    .project-visual {
      display: none;
    }

    .project-summary {
      max-width: 100%;
    }
  }

  @media (max-width: 480px) {
    :global(.mobile-bottom-nav) {
      display: none !important;
    }

    .projects-page {
      padding-top: calc(var(--hdr-h) + 48px);
    }

    .project-row-inner {
      padding: 36px 0;
    }

    .project-tools .tag {
      font-size: 9px !important;
    }

    .project-links {
      flex-direction: column;
      align-items: flex-start;
      gap: 12px;
    }
  }
</style>
