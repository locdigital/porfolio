---
import { isCmsRequestAuthorized } from "../../../lib/cms-auth";

export const prerender = false;

if (!isCmsRequestAuthorized(Astro.request)) {
  return Astro.redirect("/login");
}

return Astro.redirect(`/cms/writing/${Astro.params.id}/edit`);
---
